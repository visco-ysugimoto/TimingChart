# コマンドトリガ（Command Trigger）

コマンド指定で検査やシステム動作を起動する方式です。単接点のように 1 入力 = 1 タスクではなく、複数入力の組み合わせと補助信号 `Command Option` で動作を切り替えます。

## 特徴

- 1 接点に 1 タスクを固定しない
- コマンド用の入力と `Command Option` で実行内容を指定する
- カメラの取込順はカメラ取込表で決まる

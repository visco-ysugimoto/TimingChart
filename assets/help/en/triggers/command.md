# Command Trigger

Starts inspection or system actions by command, rather than mapping one contact to one task. Command inputs and the auxiliary signal `Command Option` select the action.

## Characteristics

- Tasks are not fixed one-to-one with contacts
- Command inputs and `Command Option` specify what to run
- Capture order is defined by the camera configuration table
